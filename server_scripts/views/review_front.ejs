<!DOCTYPE html>
<html>
<head>
	<title>Reviews</title>
	<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
</head>
<body>
	<div class="container">
		<h1 class="mb-4">Review</h1>
		<a href="/reviews/new" class="btn btn-success">Add Review</a>
		<% reviews.forEach(review =>{ %>
			<div class="card mt-4">
				<div class="card-body">
					<h4 class="card-title"><%= review.name %></h4>
					<div class="card-subtitle test-muted mb-2">
						<!-- toLocaleDateString() converts date into dd/mm/yy formate
							otherwise it shows a very big string contains india falana falana-->
						<%= review.date.toLocaleDateString() %>
					</div>
					<div class="card-text mb-2"><%= review.description %></div>
					<a href="reviews/<%=review.slug%>" class="btn btn-primary">Read More</a>
					<form action="/reviews/<%= review.id%>?_method=DELETE" method="POST" class="d-inline">
						<button type="submit" class="btn btn-danger">Delete</button>
					</form>
				</div>
			</div>
		<% }) %>
		
	</div>
</body>
</html>